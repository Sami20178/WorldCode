# WorldCode APK

Die fertige APK aus dem direkten Repository-Builder wird hier als `releases/WorldCode.apk` abgelegt.

Der GitHub-Release kann die APK zusätzlich unter **Assets** anzeigen.

Builder:

```bash
./build-apk.sh
```
